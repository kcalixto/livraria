// Campo "status" do livro no catálogo: hard-coded por hora (existirá
// semântica real no futuro); o front renderiza como tag.
export const BOOK_STATUS_AVAILABLE = 'disponível';

// Região única ativa. O valor de API não usa travessão tipográfico.
export const DEFAULT_REGION = 'SP, Capital - Zona Sul';
